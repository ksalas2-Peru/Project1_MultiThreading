This phase introduces the creation of multiple threads that run concurrently.

Each thread executes a simple task, such as printing numbers.

The goal is to understand how threads operate independently.
